#is the public template for that envelope: the same variable names but with dummy or blank values.
# It is committed, so it acts as a checklist telling you 
#(or a teammate) exactly which variables .env needs to contain — without leaking any secret.